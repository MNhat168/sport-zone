export enum BookingType {
  FIELD = 'field',
  COACH = 'coach',
  FIELD_COACH = 'field_coach',
}

export enum BookingStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled',
  COMPLETED = 'completed',
}
