# QR Checkin Environment Variables

# QR Check-in Configuration
# Secret for JWT tokens (should be different from main JWT secret for security)
QR_CHECKIN_SECRET=your-super-secret-qr-key-here-change-in-production

# Time window (in minutes) before match start time when QR can be generated
# Default: 15 minutes
QR_CHECKIN_WINDOW_MINUTES=15

# QR token expiry time (in minutes) after generation
# Default: 10 minutes
QR_TOKEN_EXPIRY_MINUTES=10
