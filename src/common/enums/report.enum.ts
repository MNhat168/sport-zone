export enum ReportType {
  USER = 'user',
  BOOKING = 'booking',
  PAYMENT = 'payment',
  REVIEW = 'review',
  COACH = 'coach',
  FIELD = 'field',
}

export enum ReportStatus {
  OPEN = 'open',
  IN_PROGRESS = 'in_progress',
  RESOLVED = 'resolved',
  CLOSED = 'closed',
}
